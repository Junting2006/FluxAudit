# FluxAudit 成员 1 + 3 + 4 联调交付说明

更新日期：2026-09-04<br>
范围：成员1测试数据与材料、成员3 Agent、成员4后端与 ChainAgent。

## 1. 已完成的联调闭环

```text
成员1 audit_request.json
    -> 成员4 POST /api/v1/audit/start
    -> ChainAgent 生成链上字段或 Mock 数据
    -> 成员3 DocAgent / CrossCheckAgent / RiskAgent
    -> 成员4 SSE 推演日志、最终报告、Merkle Root、report_hash
```

当前集成版实际验证的 SSE 业务步骤：

```text
INPUT_VALIDATED
-> CHAIN_FETCHED
-> DOC_PARSING
-> CROSS_CHECKING
-> RISK_SCORING
-> REPORT_ASSEMBLED
-> PROOF_MANIFEST_COMPILED
```

`PROOF_MANIFEST_COMPILED` 会校验前六步顺序，并承诺前六个真实哈希与业务报告正文哈希；它自身再以 Ethereum keccak256 续接 H5，形成独立 H6。Merkle Root 与 report hash 由上述完整七步链派生，并随 `COMPLETED` 终态返回，避免证明字段参与自身哈希造成循环依赖。

## 2. 每位成员已完成内容

| 成员 | 交付内容 | 联调中发挥的作用 |
|---|---|---|
| 成员1 | NovaPay / AtlasIndex 两套白皮书、PDF、audit_request.json、事实速查表、路演材料与合规文档 | 提供稳定、可复现的演示输入和预期结果 |
| 成员3 | DocAgent、CrossCheckAgent、RiskAgent、risk_score、findings、reasoning_log、step_hash | 解析文档、核验链上字段、输出结构化风险结论 |
| 成员4 | FastAPI、SSE、ChainAgent、Mock Fallback、报告构造、Merkle Root、report_hash | 编排任务并提供可供前端和成员5消费的 API / JSON |

## 3. 已验证结果

| 检查项 | 结果 |
|---|---|
| 成员3 Agent 单元测试 | 68 PASS，0 FAIL |
| 成员4 ChainAgent 测试 | 26 PASS，0 FAIL |
| 成员4 Hashing 测试 | 27 PASS，0 FAIL |
| 成员4 API 闭环测试 | 39 PASS，0 FAIL |
| 成员2前端 Sites 测试 | 4 PASS，0 FAIL；生产构建通过 |
| NovaPay 端到端结果 | 85 / HIGH / 6 findings |
| AtlasIndex 端到端结果 | 2 / LOW / 1 finding（仅 DOC_LAG） |

NovaPay 演示必须使用：

```text
0x71C7656EC7ab88b098defB751B7401B5f6d8976F
```

AtlasIndex 演示必须使用：

```text
0xA2b8C9d4E7f1029384756A1B2C3D4E5F60718293
```

这两个地址会加载固定的 Mock 链上数据。换成其他地址会进入通用 Mock 分支，分数与 findings 不保证一致。

## 4. 如何运行

Windows PowerShell：

```powershell
cd ".\member4"
python -m pip install -r .\requirements.txt
python app.py
```

浏览器入口：

- 健康检查：http://localhost:8000/api/v1/health
- Swagger 演示页：http://localhost:8000/docs

健康检查应显示：

```json
{
  "status": "ok",
  "use_mock_data": true,
  "chain_agent_status": "mock",
  "member3_connected": true
}
```

现场优先粘贴 audit_request.json 中的 contract_address 和 whitepaper_text；不要依赖拖入 PDF，PDF 只作为展示附件。

## 5. 成员2需要注意

1. 前端调用 POST /api/v1/audit/start 后保存 task_id。
2. 使用 GET /api/v1/audit/stream/{task_id} 展示 SSE 推演步骤。
3. 使用 GET /api/v1/audit/report/{task_id} 展示 summary、findings、七步 `steps`、reasoning_step_hashes、manifest_step_hash、merkle_root、report_hash。
4. 页面必须诚实展示 chain_agent_status=mock，不能把合成数据写成实时主网数据。
5. 前端验伪页需要保留“篡改报告后验证失败”的演示路径。

## 6. 成员5需要注意

1. 需要部署 Sepolia Attestation 合约，并记录合约地址、交易哈希和区块浏览器链接。
2. 上链字段为 reportHash、merkleRoot、riskScore，分别来自报告的 proof_data.report_hash、proof_data.merkle_root、summary.overall_risk_score。
3. 必须和 Python 使用同一 Ethereum keccak256 口径，不能使用 NIST SHA3-256。
4. canonical JSON 必须递归将整数型浮点数规范成整数（如 `2.0 → 2`）后再排序键并紧凑序列化；字符串、布尔值、`null` 与非整数小数保持不变。
5. 必须复现固定测试向量：

```text
keccak256("fluxaudit-test")
= 0xcce8555ed74ef8887a1250c7048b9910d0e5fdd755836f4ffe4f4059a7fef103
```

6. 需要完成两条验证：原报告验证通过；改动报告任意数字后验证失败。
7. 私钥、助记词、RPC Key 不能进入仓库、压缩包、PPT、录屏或群聊。

## 7. 还未完成的工作

| 项目 | 负责人 | 当前状态 |
|---|---|---|
| Sepolia 合约部署、锚定交易与验伪 | 成员5 | 未完成，需要保留交易哈希 |
| 前端原型接真实后端与 Sepolia 验伪 | 成员2 + 成员5 | 界面原型已完成，真实联调未完成 |
| 系统架构图 PNG | 成员4 | 未完成 |
| 3 分钟以内连续演示视频 | 成员1 + 成员5 | 未完成 |
| 全组至少两次完整彩排 | 全组 | 未完成 |

## 8. 当前边界

- 当前演示默认是 Mock 链上数据。真实 RPC / Etherscan 失败、缺 Key 或限流时会自动降级；必须在答辩中如实披露。
- DeepSeek 只用于可选的文档结构化解析；无 Key 或超时时会回退规则引擎。真实 DeepSeek API 尚未作为现场必需依赖验证。
- task_queue 是内存实现，重启后任务、日志、报告会清空；演示环境无需持久化。
- 报告仅为辅助风险诊断，不构成投资、法律或合规意见。
