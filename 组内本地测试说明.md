# FluxAudit 组内一键测试说明

版本基线：Git commit `adf92ea`  
测试目标：在本地完成 `Create audit → 7-step execution → Report → Local Verify` 全流程。

## 1. 环境要求

- Node.js `>= 22.13`
- npm（随 Node.js 安装）
- Python `>= 3.10`
- 首次安装依赖时需要联网
- 不需要钱包、私钥、DeepSeek Key 或 Etherscan Key 即可完成本地闭环

请先解压整个文件包，再运行命令；不要直接在压缩包预览窗口中启动。

## 2. 一键启动（推荐）

macOS：双击 `start-macos.command`。如果系统阻止双击，请在终端执行：

```bash
bash start-macos-linux.sh
```

Linux：

```bash
bash start-macos-linux.sh
```

Windows：双击 `start-windows.bat`。

第一次运行会自动创建隔离的 Python 环境并安装锁定的前端依赖，通常需要几分钟。看到 `已就绪：http://127.0.0.1:4173/` 后即可测试；浏览器通常会自动打开。保持启动窗口开启，按 `Ctrl+C` 会同时停止前后端。

启动器固定使用本地 Mock 数据、连接 member3，并且只监听本机 `127.0.0.1`，不会读取或输出钱包私钥、助记词和 API Key。

## 3. 手动启动（备用）

### 启动后端

在第一个终端进入解压后的项目目录。

### macOS / Linux

```bash
cd member4
python3 -m venv .venv
source .venv/bin/activate
python -m pip install -r requirements.lock.txt
python -m uvicorn app:app --host 127.0.0.1 --port 8000
```

### Windows PowerShell

```powershell
cd member4
py -m venv .venv
.\.venv\Scripts\Activate.ps1
python -m pip install -r requirements.lock.txt
python -m uvicorn app:app --host 127.0.0.1 --port 8000
```

看到服务运行在 `http://127.0.0.1:8000` 后，打开健康检查：

<http://127.0.0.1:8000/api/v1/health>

预期：`member3_connected` 和 `use_mock_data` 均为 `true`。

### 启动前端

保持后端终端运行，在第二个终端进入项目的 `frontend` 目录：

```bash
npm ci
npm run dev -- --host 127.0.0.1 --port 4173 --strictPort
```

打开：<http://127.0.0.1:4173/>

## 4. 必测完整流程

1. 首页选择 `NovaPay`，点击开始审计。
2. 执行页应依次完成七个步骤：
   `INPUT_VALIDATED → CHAIN_FETCHED → DOC_PARSING → CROSS_CHECKING → RISK_SCORING → REPORT_ASSEMBLED → PROOF_MANIFEST_COMPILED`。
3. 每一步都应显示 Ethereum `keccak256` 哈希，最终进度为 `7 / 7`。
4. 报告页预期为 `85 / HIGH / 6 findings`，本地证明状态为 `VERIFIED`。
5. 点击 `Download JSON` 下载报告，再进入 `Verify report`，运行本地验证；四项检查应全部通过。
6. 复制下载的 JSON，任意修改一个风险分或 finding 后重新上传；验证必须失败，不能显示绿色通过。
7. 返回首页选择 `AtlasIndex` 再跑一次；预期为 `2 / LOW / 1 finding`，且不能残留 NovaPay 的步骤、报告或风险状态。

默认未配置真实 Sepolia 合约，因此链上状态显示 `NOT CONFIGURED` 是正确结果，不是故障。不要为了变绿而伪造交易号、区块号或合约地址。

## 5. 自动化测试

前端与 member5：

```bash
cd frontend
npm test
npm run build

cd ../member5
npm ci --ignore-scripts
npm test
```

成员3与成员4请回到测试包根目录。一键启动会把 Python 虚拟环境创建在根目录的 `.runtime/venv`；如果你使用第 3 节手动启动，则把下面命令中的 `.runtime/venv` 替换为 `member4/.venv`。

macOS / Linux：

```bash
./.runtime/venv/bin/python member3/test_agent_process.py
./.runtime/venv/bin/python member3/test_llm_fallback.py
./.runtime/venv/bin/python member4/tests/test_hashing.py
./.runtime/venv/bin/python member4/tests/test_chain_agent.py
./.runtime/venv/bin/python member4/tests/test_api.py
```

Windows PowerShell：

```powershell
.\.runtime\venv\Scripts\python.exe member3\test_agent_process.py
.\.runtime\venv\Scripts\python.exe member3\test_llm_fallback.py
.\.runtime\venv\Scripts\python.exe member4\tests\test_hashing.py
.\.runtime\venv\Scripts\python.exe member4\tests\test_chain_agent.py
.\.runtime\venv\Scripts\python.exe member4\tests\test_api.py
```

`member3/test_tell_llm_success_path.py` 是真实 DeepSeek 联网路径，只有在明确配置测试 Key、允许发送演示文本到第三方服务时才执行；默认本地闭环不需要它。

基线结果：前端 `30 PASS`，member5 `95 PASS`，成员3 `68 PASS`，成员4 `93 PASS`，均应为 `0 FAIL`。

## 6. 常见问题

- 首页显示后端离线：确认一键启动窗口仍在运行，或确认手动启动的 `python -m uvicorn app:app ...` 进程仍在运行，并检查 8000 端口是否被占用。
- 前端打不开：确认命令在 `frontend` 目录执行，且使用的是 Node.js 22.13 或更高版本。
- `npm ci` 失败：确认完整解压了 `frontend/vendor/fluxaudit-member5-web3-0.2.0.tgz`。
- 端口 4173 已占用：先结束占用进程；不要随意换端口，否则需要同步检查代理与浏览器地址。
- Windows 禁止激活脚本：可在 `member4` 目录直接使用 `.\.venv\Scripts\python.exe -m uvicorn app:app --host 127.0.0.1 --port 8000` 启动后端。
- 链上状态未配置：当前交付包没有真实 Sepolia 部署记录，这是预期边界。

## 7. 安全说明

- 包内没有 `.env`、API Key、私钥或助记词。
- `.env.example` 只能作为字段模板；不要把真实私钥写入任何 `VITE_` 变量。
- 当前默认走确定性 Mock 链上数据和本地规则引擎，适合稳定演示；界面会如实标记数据来源。
- 报告仅用于黑客松测试，不构成投资、法律或财务建议。

## 8. 反馈格式

发现问题时请提供：操作系统、Node/Python 版本、复现步骤、页面截图、浏览器 Console 错误和终端最后 30 行日志。不要发送私钥、助记词或真实 API Key。
