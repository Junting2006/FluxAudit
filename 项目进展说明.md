# FluxAudit 项目进展说明

更新时间：2026-09-05  
适用范围：AIX Origin Summit 2026 当前 checkpoint 上传版本

## 1. 项目当前状态

FluxAudit 当前已经从单点模块开发推进到可本地运行的端到端原型。系统可以用成员1准备的项目材料作为输入，通过成员4后端调度成员3 Agent 完成文档解析、链上事实交叉核验和风险评分，再由前端展示执行过程、报告结果和本地验伪结果。

当前版本重点证明三件事：

1. AI 风险分析不是只输出文本，而是输出结构化、可复现的风险报告。
2. 每一步分析过程可以生成 step hash、Merkle Root 和 report hash，便于后续链上锚定。
3. 前端、后端、Agent 和 Web3 验伪 SDK 已经能在本地形成完整演示闭环。

## 2. 已完成部分

### 2.1 成员1：产品材料与测试数据

已完成两套稳定演示样例：

| 样例 | 目标 | 预期结果 |
| --- | --- | --- |
| NovaPay | 展示白皮书声明与链上数据存在明显矛盾 | 85 / HIGH / 6 findings |
| AtlasIndex | 展示系统不会把所有项目都误判为高风险 | 2 / LOW / 1 finding |

已完成内容包括：

- 两套 `audit_request.json` 测试输入。
- 两套白皮书 PDF 和链上事实速查表。
- 路演演讲稿、演示串场词、项目说明、测试记录、提交材料清单。
- 免责声明、PDPO 隐私合规说明和 LICENSE 说明。

### 2.2 成员3：AI Agent 与风险评分

成员3已经完成核心 Agent 流程：

- `DocAgent`：从项目文档中抽取代币分配、锁仓、审计声明、路线图等信息。
- `CrossCheckAgent`：将文档声明与链上事实、Mock 链上数据进行交叉核验。
- `RiskAgent`：根据风险规则输出风险分、风险等级、findings 和建议。
- 支持 `reasoning_log` 和 `step_hashes`，为前端 SSE 展示和后续 Merkle Root 计算提供依据。
- 支持可选 DeepSeek 文档读取路径；未配置 API Key 时仍可走确定性规则和本地 Mock 流程。

当前成员3入口为：

```python
member3/agent_process.py::run_agent_process(input_data)
```

### 2.3 成员4：后端、任务编排与可验证报告

成员4已经完成 FastAPI 后端闭环：

- `POST /api/v1/audit/start`：提交审计任务。
- `GET /api/v1/audit/stream/{task_id}`：通过 SSE 返回执行步骤。
- `GET /api/v1/audit/report/{task_id}`：返回最终报告、Merkle Root 和 report hash。
- `GET /api/v1/health`：检查后端、Mock 模式和成员3连接状态。

已完成的关键能力：

- 自动调用成员3 Agent。
- 对 EVM 地址做基础校验，非法地址会在任务创建前被拒绝。
- 使用 Ethereum `keccak256`，避免误用 NIST SHA3-256。
- 为每个审计步骤生成 hash，并构造 Merkle Root。
- 使用 Mock Fallback 保证黑客松演示不依赖不稳定外部链上接口。

### 2.4 成员2：前端 UI/UX 与本地演示闭环

成员2前端已经完成主要页面：

- 首页 / Create audit：选择 NovaPay 或 AtlasIndex 并发起审计。
- Execution：展示七步执行过程和 SSE 状态流。
- Report：展示风险分、风险等级、findings、证据和建议。
- Verify：执行本地验伪，验证报告结构、步骤 hash、Merkle Root 和 report hash。

当前前端已经能在本地完成：

```text
Create audit -> 7-step execution -> Report -> Local Verify
```

Sepolia 未配置时，前端默认显示 `NOT CONFIGURED`，不会伪造链上交易、区块号或浏览器链接。

### 2.5 成员5：Web3 SDK、合约与验伪规则

成员5已经合入：

- Solidity `Attestation` 合约。
- ABI、Hardhat 配置、部署脚本和本地 demo 脚本。
- `@fluxaudit/member5-web3` SDK 0.2.0。
- 严格七步验伪逻辑、canonical JSON、hashing、fixtures 和测试。
- 前端已经通过固定 vendor 包调用 member5 SDK 做本地验伪。

当前已做到本地严格验伪，但真实 Sepolia 部署记录、链上写入和区块浏览器验证仍未完成。

### 2.6 测试与工程整理

当前 README 和测试说明中记录的基线结果：

| 模块 | 当前结果 |
| --- | --- |
| 成员3 Agent | 68 PASS，0 FAIL |
| 成员4 ChainAgent | 26 PASS，0 FAIL |
| 成员4 Hashing | 27 PASS，0 FAIL |
| 成员4 API 闭环 | 40 PASS，0 FAIL |
| 成员2前端 Proof / Task scope / member5 集成 / Sites 测试 | 30 PASS，0 FAIL，生产构建通过 |
| 成员5 SDK / Solidity 合约 | 95 PASS，0 FAIL |
| 浏览器真实闭环 | NovaPay、AtlasIndex、七步 SSE、报告与本地 Verify 均通过 |

另外，已经整理出适合 GitHub 上传的干净目录：

```text
FluxAudit_github_upload_20260905_adf92ea/
```

该目录排除了 `.runtime/`、`node_modules/`、`__pycache__/`、真实 `.env`、日志、构建产物和压缩包，只保留源码、文档、测试、fixtures、锁文件和必要演示资产。

## 3. 剩余待完成部分

### 3.1 P0：真实 Sepolia Attestation 闭环

负责人：成员5，成员2配合前端接入。

待完成：

- 部署 Sepolia `Attestation` 合约。
- 保存合约地址、部署交易哈希、区块浏览器链接。
- 用真实报告写入 `reportHash`、`merkleRoot`、`riskScore`。
- 写入后执行 read-back，确认链上读取结果与本地报告一致。
- 将可信配置写入前端环境变量或部署配置。
- 在 Verify 页面展示真实链上状态，而不是 `NOT CONFIGURED`。

验收标准：

- 有固定合约地址。
- 有至少一笔可公开查询的测试交易。
- 本地报告 hash 与链上读取结果一致。
- 前端不能依赖假交易号或手填结果。

### 3.2 P0：最终 GitHub 上传

负责人：组长。

当前本地仓库已经准备完成，但还缺目标 GitHub 仓库 URL 或已登录的 GitHub CLI / token。

待完成：

- 确认目标 GitHub repo。
- 添加 remote：`git remote add origin <repo-url>`。
- 推送 main 分支：`git push -u origin main`。
- 上传后检查 GitHub 页面是否正确显示 README、项目结构、中文文件名和图片资产。

### 3.3 P1：系统架构图与最终提交材料

负责人：成员1、成员4。

待完成：

- 输出一张清晰的系统架构图 PNG。
- 图中明确展示：前端、FastAPI、成员3 Agent、Mock / Etherscan 数据源、member5 SDK、Sepolia Attestation 合约。
- 更新提交材料清单，确保 GitHub、演示视频、测试记录、合规说明都可访问。

### 3.4 P1：3 分钟连续演示视频

负责人：成员1主导，全组配合。

待完成：

- 录制不超过 3 分钟的完整演示。
- 建议流程：NovaPay 高风险审计 -> 报告 -> 本地验伪 -> AtlasIndex 低风险对照 -> 如 Sepolia 完成则展示链上验证。
- 录屏前做至少两次完整彩排，避免端口、依赖安装、API Key 或链上余额问题影响展示。

### 3.5 P1：README 与测试记录最终对齐

负责人：组长。

待完成：

- Sepolia 完成后更新 README 中的链上状态说明。
- 更新测试记录中的最终 pass 数、浏览器截图和演示结果。
- 确认所有文档中的数字一致，例如 API 测试数量、前端测试数量、member5 测试数量。

## 4. 后续计划

### 4.1 今天内优先计划

1. 完成 GitHub remote 配置并推送当前干净版本。
2. 成员5部署 Sepolia 合约并提交交易哈希、合约地址、Etherscan 链接。
3. 成员2接入真实 Sepolia 配置，验证前端 read-back 状态。
4. 组长更新 README、测试记录和提交材料清单。

### 4.2 提交前计划

1. 跑一遍自动化测试：成员3、成员4、前端、member5。
2. 跑一遍浏览器手动闭环：NovaPay 和 AtlasIndex 各一次。
3. 做最终 secret scan，确认没有私钥、助记词、真实 API Key、钱包敏感信息进入仓库或录屏。
4. 录制最终 3 分钟 demo 视频。
5. 检查 GitHub 仓库首页、README、图片、PDF、中文路径是否正常展示。

### 4.3 如果 Sepolia 来不及完成

保底提交策略：

- 明确说明当前已完成本地严格验伪。
- 前端保持 `NOT CONFIGURED`，不伪造链上结果。
- 在路演中把 Sepolia Attestation 作为 next step，而不是假装已经完成。
- 重点展示 AI 审计闭环、SSE 可解释过程、Merkle Root / report hash 和本地 tamper detection。

## 5. 遇到的技术难点

### 5.1 AI 输出与确定性审计之间的边界

难点：如果完全依赖 LLM 输出，风险判断可能不稳定，演示结果也不容易复现。  
处理方式：将 DeepSeek 设为可选文档结构化能力，核心交叉核验、风险评分和 hash 生成仍由确定性规则完成。

### 5.2 Ethereum keccak256 与 SHA3-256 的差异

难点：Python 和 JavaScript 生态里容易把 NIST SHA3-256 和 Ethereum keccak256 混用，导致前后端、SDK、合约计算出的 hash 不一致。  
处理方式：统一使用 Ethereum `keccak256`，并保留固定测试向量，保证 member3、member4、member5 之间 hash 结果一致。

### 5.3 报告可验证性设计

难点：普通 AI 报告只能阅读，无法证明报告内容和分析步骤有没有被篡改。  
处理方式：为每个分析步骤生成 step hash，再构造 Merkle Root 和 report hash；前端 Verify 页面用 member5 SDK 重新计算并拒绝被篡改的报告。

### 5.4 前后端实时状态同步

难点：审计过程需要逐步展示，而不是等后端一次性返回最终结果。  
处理方式：成员4提供 SSE stream，前端 Execution 页面按步骤展示执行状态，并避免 NovaPay 与 AtlasIndex 两个任务之间残留旧状态。

### 5.5 链上集成的真实性边界

难点：黑客松演示很容易为了界面完整而写死交易号或伪造链上状态，但这会损害可信度。  
处理方式：真实 Sepolia 未配置时前端明确显示 `NOT CONFIGURED`；只有拿到固定合约地址、RPC、trusted auditor 和 read-back 结果后，才开启链上验证。

### 5.6 本地一键测试包与 GitHub 仓库格式不同

难点：一键测试包为了方便组员运行，包含 `.runtime`、`node_modules` 等本地依赖；这些内容不适合直接上传 GitHub。  
处理方式：另建 GitHub 上传目录，排除可重建依赖和本地缓存，保留源码、锁文件、测试、fixtures、文档和演示材料，并添加 `.gitignore` 与 `.gitattributes`。

## 6. 当前风险与应对

| 风险 | 影响 | 应对 |
| --- | --- | --- |
| Sepolia 部署未完成 | 无法展示真实链上锚定 | 优先处理；若来不及，明确作为未完成项，不伪造结果 |
| GitHub 尚未推送 | 无法提交代码链接 | 等目标 repo URL 后立即 push |
| 演示依赖本地端口和环境 | 彩排时可能启动失败 | 使用 `start-windows.bat` 或 `launch_fluxaudit.py`，提交前至少两次彩排 |
| 文档测试数字可能随着最后修改变化 | 评委看到的结果不一致 | 最终提交前统一 README、测试记录、进展说明 |
| API Key / 私钥泄露 | 严重合规与安全问题 | 只提交 `.env.example`，提交前执行 secret scan |

## 7. 当前结论

FluxAudit 当前已经具备可展示的本地 MVP：输入、AI 审计、后端编排、实时执行、风险报告、本地验伪和演示数据均已打通。剩余工作的核心不是重新开发主流程，而是完成真实 Sepolia 证明、GitHub 上传、架构图、最终测试记录和演示视频。

建议接下来按优先级推进：

```text
GitHub 上传 -> Sepolia 部署与 read-back -> README / 测试记录对齐 -> 架构图 -> 3分钟演示视频 -> 最终彩排
```
